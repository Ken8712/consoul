<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Consoul" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP&display=swap" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital@1&display=swap" rel="stylesheet">
    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    
    
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              'pink': {
                400: '#f472b6',
                500: '#ec4899',
                600: '#db2777'
              }
            }
          }
        }
      }
    </script>
    
    <!-- iOS最適化 -->
    <style>
      .bg-main-iphone {
        background-image: url("<%= asset_path('bg-main.png') %>");
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        min-height: 100vh;
        min-width: 100vw;
      }
      /* タップハイライト無効化 */
      * {
        -webkit-tap-highlight-color: transparent;
      }
      
      /* タッチ操作の最適化 */
      .touch-manipulation {
        touch-action: manipulation;
      }
      
      /* ボタンのダブルタップズーム無効化 */
      button {
        touch-action: manipulation;
      }
      
      /* スクロールの最適化 */
      body {
        -webkit-overflow-scrolling: touch;
        touch-action: none;
      }

      .playfair-display-i400 {
      font-family: "Playfair Display", serif;
      font-optical-sizing: auto;
      font-weight: 400;
      font-style: italic;
      <%# color: #0facbc; %>
      color: rgba(15, 172, 188, 0.6);
      text-shadow:
    0 0 3px #0facbc,
    0 0 5px #0facbc,
    0 0 10px #0facbc,
    0 0 13px #0facbc;
      }

      .icon-logout { 
        height: 5px;
      }
    </style>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-black bg-main-iphone">

    <!-- Phirealityヘッダー -->

    <nav class="absolute top-0 left-0 right-0 bg-gradient-to-b from-black/100 to-black/1 backdrop-blur-md z-50">
      <div class="max-w-7xl mx-2 sm:px-6 lg:px-8">
        <div class="flex justify-between">
            <%# ヘッダーロゴ %>
          <div class="flex items-center">
            <%= link_to root_path, class: "text-xl playfair-display-i400" do %>
              Φhilight
            <% end %>
          </div>
          
          <% if user_signed_in? %>
            <div class="playfair-display-i400 flex items-center space-x-1">
              <span class="playfair-display-i400 text-xs">
                <!-- <%= current_user.name %>さん -->
                connected
                </span>
                <% if current_user.paired? %>
                  <span class="playfair-display-i400 text-xs">
                  <!-- <%= current_user.partner.name %> -->
                  / paired
                  </span>
                <% else %>
                  <span class="text-gray-400">(ペアなし)</span>
                <% end %>

              <%= link_to destroy_user_session_path, 
                          data: {turbo_method: :delete},
                          class: "text-sm text-gray-600 hover:text-gray-900" do %>
                | Sign-out
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    </nav>

    <!-- フラッシュメッセージ -->
    <% if notice %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mx-4 mt-4">
        <%= notice %>
      </div>
    <% end %>
    
    <% if alert %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mx-4 mt-4">
        <%= alert %>
      </div>
    <% end %>

    <!-- メインコンテンツ -->
    <main class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8 bg-main-iphone">
      <%= yield %>
    </main>

    <svg display: none;>
    
  </svg>
  </body>





</html>
